# vim: ft=python

sourceTargets = ['main.cpp','Figure.cpp','Space.cpp']

env = Environment(
                    CCFLAGS = '-O2 -std=c++0x',
                    LINKFLAGS = '-std=c++0x'
                 )

env.Program('Intersector',sourceTargets)
